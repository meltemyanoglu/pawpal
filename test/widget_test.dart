import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('PawPal app smoke test', (WidgetTester tester) async {
    // Placeholder — real tests require SharedPreferences setup
    expect(true, isTrue);
  });
}
